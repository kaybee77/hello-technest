# hello-technest (app)

Vite + React source for the **hello-technest** project.

Documentation for the repo (deploy, stack, local run) lives in the **[root README](../README.md)**.

```bash
npm install
npm run dev
```
